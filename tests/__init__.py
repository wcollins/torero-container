"""
Test package for torero-api

This package contains test modules for verifying the functionality
of the torero-api service. It includes both unit and integration tests
to ensure the API correctly interfaces with the torero CLI and properly
implements the RESTful interface.
"""